// Esta tela existe apenas para colocar "Conversas" dentro das tabs.
// Em vez de usar Redirect, ela reaproveita a tela real de conversas.
// Isso evita erro ao tocar no ícone da tab.

export { default } from '../conversas';
